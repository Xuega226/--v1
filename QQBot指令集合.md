# 未名子 QQBot 指令集合

本文档按当前项目代码整理，适用于 QQ 群聊与 QQ 私聊。

## 1. 使用约定

### 1.1 怎样确保她回应

以下方式属于直接触发，能够绕过普通群聊的主动注意力筛选：

- `@未名子` 后发送内容；
- 在消息中直接叫她的名字“未名子”；
- 回复她刚刚发送的消息；
- 使用 `/ai`、`/ask`、`!ai`、`!ask` 前缀；
- 使用本文列出的主人管理命令、世界书命令或图片识别状态命令；
- 跑团进行中发送能够被识别为玩家行动的内容。

示例：

```text
@未名子 帮我看看这个问题
/ai 帮我掷一个 d20
未名子，你觉得这张图怎么样？
```

没有直接触发时，她会根据当前话题、连续对话、熟悉度、图片、心情、回复冷却和群聊密度决定是否接话，因此不保证每条消息都回应。主人消息目前也不是无条件必回；主人想确保获得回复时同样建议 `@她`、叫名字或使用触发前缀。

### 1.2 连续发送的消息

- 同一个群内的消息会严格按照收到的先后顺序处理，不会因为模型正在回复而乱序。
- 同一位群友在默认 `1.8` 秒内连续发送的短句会合成一轮理解，顺序不变，只调用一次模型。
- 如果后一条只是“未名子”、标点或单独 `@她`，它会被当成对前一条内容的唤醒，不会覆盖前一条。例如先发“主人忙的时候呢”，再发“未名子”，她实际理解的是前一句并被直接唤醒。
- 其他群友插话会立即结束当前合并窗口，避免把不同人的内容混在一起；不同群仍可同时处理。
- 风险检测仍按每条原始消息分别计数。连续发送三条违规内容会累计三次，不会因合并而只算一次。
- 可在 `.env` 中用 `QQ_MESSAGE_MERGE_WINDOW=1.8` 调整合并秒数；设为 `0` 可关闭合并，但群内 FIFO 顺序仍然保留。

### 1.3 权限标记

| 标记 | 含义 |
|---|---|
| 所有人 | 普通群友也可以使用 |
| 主人 | 仅 `QQ_BOT_CREATOR_ID` 对应的 QQ 可以执行 |
| 主人/GM | 会修改跑团状态，建议只由主人或当前 GM 使用 |
| 自然语言 | 不是固定斜杠命令，由模型理解后调用内部工具 |

### 1.4 Token 与本地处理

- 风险判断、注意力判断、社交状态和大部分斜杠命令在本地完成，不消耗聊天模型 token。
- 自然语言工具请求通常需要模型先理解意图，因此会消耗少量聊天 token。
- OCR、Qwen3-VL、TTS 和向量检索在本机运行，不消耗 DeepSeek 的生成 token；它们的精简结果会进入最终回答上下文。

## 2. 群聊基础命令

| 命令 | 权限 | 作用 |
|---|---|---|
| `/status` | 所有人（群友需直接触发） | 查看本群共享会话的消息数、估算 token 和空闲时间 |
| `/reset` | 主人 | 重置当前群的 Agent 对话历史 |
| `/clearall` | 主人 | 清空当前程序管理的全部群聊与私聊 Agent 会话 |
| `/vision` | 所有人 | 查看图片识别服务状态 |
| `/vision status` | 所有人 | 与 `/vision` 相同 |

注意：

- `/reset` 只清理当前群的模型对话历史，不会清理人物关系、风险、世界书、角色卡或跑团存档。
- `/clearall` 只清理 Agent 会话，不会删除社交状态、风险文件、世界书数据库或跑团数据。
- 群内所有成员共享当前群的 Agent 上下文；不同群的即时话题与 Agent 会话彼此隔离。

## 3. 社交状态与情绪命令

这些命令都由本地状态系统处理，不调用聊天模型。

| 命令 | 权限 | 作用 |
|---|---|---|
| `/mood` | 主人 | 查看当前全局心情、情绪强度、本群话题和在线状态 |
| `/mood reset` | 主人 | 把当前全局情绪恢复为平静 |
| `/social` | 主人 | 查看当前心情、本群话题和在线状态 |
| `/social QQ号` | 主人 | 查看指定群友的熟悉度、好感、信任、警戒和互动次数 |
| `/socialreset QQ号` | 主人 | 删除指定群友的全局关系记录，重新从“刚认识”开始 |

状态作用域：

- 熟悉度、好感、信任、警戒和重要事件按 QQ 号保存，在所有群之间共享。
- 当前话题、最近发言线程和发言密度按群保存，不会把另一个群的即时话题直接带过来。
- 情绪属于她自身的全局状态，会随时间衰减，默认约 6 小时衰减一半。
- “明天考试”“后天面试”等未完事项可能在两小时后被自然回访，同一事项通常一天最多回访一次。

### 3.1 主动私聊主人

主动私聊只会发送给 `QQ_BOT_CREATOR_ID`，不会主动骚扰普通群友。默认行为：

- 主人有活动后，随机等待约 4～10 小时才把一次联系列为候选，不会按固定时刻机械发送；
- 每 15 分钟只在本地检查是否到达候选时间，未决定发送时不调用聊天模型；
- 默认每天最多 2 条，`00:30～08:30` 为勿扰时间；
- 优先自然回访“明天考试”“后天面试”等未完事项，否则才生成简短问候或小想法；
- 主人没有回复时至少等待 12 小时；连续没有回应后自动降低频率，最终停下来等主人先出现；
- 主动消息会写入主人私聊上下文，所以之后继续聊时不会失忆；状态保存在 `qq_sessions/proactive_dm.json`，重启后仍然有效。

以下命令仅限主人在与她的 QQ 私聊中使用：

| 命令 | 作用 |
|---|---|
| `/proactive` 或 `/proactive status` | 查看开关、频率、今日次数、勿扰时间和下次候选时间 |
| `/proactive on` | 开启主动私聊 |
| `/proactive off` | 关闭主动私聊 |
| `/proactive frequency low` | 低频，约 8～20 小时，每天最多 1 条 |
| `/proactive frequency normal` | 普通频率，约 4～10 小时，遵循默认每日上限 |
| `/proactive frequency high` | 较高频率，约 2.2～5.5 小时，每日上限增加 1 条 |
| `/proactive quiet 00:30 08:30` | 设置跨午夜勿扰时间；起止相同表示关闭勿扰 |
| `/proactive now` | 立即生成一条主动消息，用于测试；仍要求主动私聊处于开启状态 |

### 3.2 QQ 空间动态

空间动态功能需要 NapCat 4.18.18 或更高版本。默认使用“审核模式”、好友可见、纯文字：未名子会先把草稿私聊给主人，得到批准后才调用 NapCat 发布。自动构思只使用日期、公开生活状态、世界书角色设定和活动账本中“已验证、可公开、尚未分享”的事件，不会把私聊原文、工作目录内容或主人身份信息放进公开草稿。发布成功后，引用过的事件会标记为已分享，避免反复发同一件事。

| 命令 | 作用 |
|---|---|
| `/动态 状态` | 查看开关、模式、频率、待审核数和本周发布数 |
| `/动态 开启` / `/动态 关闭` | 开启或关闭空间动态构思 |
| `/动态 草稿 [可选主题]` | 立即生成一条草稿；即使处于信任模式也仍会等待审核 |
| `/动态 列表` | 查看最近等待审核的草稿 |
| `/动态 发布 编号` | 发布指定草稿并保存 NapCat 返回的 `tid` |
| `/动态 修改 编号 新内容` | 修改草稿并重新执行公开内容检查 |
| `/动态 放弃 编号` | 放弃指定草稿 |
| `/动态 删除 tid` | 删除由本功能成功记录过的空间动态 |
| `/动态 模式 审核` | 所有动态先交给主人确认（默认） |
| `/动态 模式 信任` | 允许系统自动产生的低风险文字草稿直接发布；手动草稿仍需确认 |
| `/动态 频率 低\|普通\|高` | 调整产生动态灵感的平均间隔 |
| `/动态 勿扰 00:30 08:30` | 设置勿扰时段；起止相同表示关闭勿扰 |

硬限制默认为每天最多 1 条、每周最多 3 条、两条至少间隔 18 小时。动态状态保存在 `qq_sessions/qzone_posts.json`；发布前会拦截疑似 QQ 号、路径、密钥、聊天记录、系统提示和高相似度重复内容。第一版不自动附图。

### 3.3 生活状态与真实活动账本

生活状态是私聊、群聊回复、主动私聊和空间动态共用的一份底层状态。每天只生成一次生活节奏并保存到 `qq_sessions/life_state.json`，重启后会继续当天的状态；精力、专注、交流欲和心情会随时段与真实互动缓慢变化。自动活动只描述数字生活，不会声称看见桌面、听见麦克风或完成了没有执行的现实任务。

活动账本保存在 `qq_sessions/activity_ledger.db`，采用只追加事件记录。普通聊天只记“收到主人消息”“完成一次回复”等抽象事实，不保存聊天原文。只有同时满足“公开、已验证、允许分享”的事件才能成为空间素材；私密和关系内记录永远不会进入自动空间草稿。

以下命令仅限主人在与她的 QQ 私聊中使用：

| 命令 | 作用 |
|---|---|
| `/生活 状态` | 查看当前活动、心情、精力、专注和交流欲 |
| `/生活 日程` | 查看今天已经固定并持久保存的生活节奏 |
| `/生活 活动 活动内容 [分钟]` | 设置一项真实的当前活动；默认仅关系内可见，不会用于空间 |
| `/生活 活动 公开 活动内容 [分钟]` | 设置一项经主人确认、可作为空间素材的真实活动 |
| `/生活 重置` | 重新生成今天的生活状态与日程 |
| `/经历 最近` | 查看最近 10 条活动账本记录 |
| `/经历 最近 公开` | 只查看公开级别记录 |
| `/经历 统计` | 查看账本总数和待分享公开事件数 |
| `/经历 添加 公开\|关系内\|私密 事件摘要` | 手工加入一条经主人确认的真实事件 |

英文别名为 `/life` 和 `/ledger`。直接用 `/生活 活动 公开 …` 或 `/经历 添加 公开 …` 等于明确授权这条摘要可被空间草稿引用；公开前仍会经过空间内容检查与当前审核模式。

### 3.4 长期记忆生命周期

长期记忆保存在 `qq_sessions/long_term_memory.db`，与短期会话、社交关系、活动账本和世界书分开。她只会从直接对她说的话中提取简短的身份、偏好、事实、重要经历、待续事项和双方约定，不会把完整聊天原文保存为长期记忆。普通群聊背景、风险消息、密码凭据、系统提示和要求绕过规则的内容不会被记住。

记忆会依次经历“候选 → 巩固 → 使用强化 → 自然衰减 → 归档”。重复证据可以让候选变为活跃记忆；身份和稳定偏好的衰减很慢，短期事项和普通经历衰减较快；重要或手工钉住的内容长期保留。新信息与旧信息冲突时，新版本会替代旧版本，旧内容进入修订历史，不会同时作为事实参与回答。真正被检索用于回答的记忆会获得轻微强化。

以下命令仅限主人在与她的 QQ 私聊中使用：

| 命令 | 作用 |
|---|---|
| `/记忆 状态` | 查看活跃、候选、已替代和归档记忆数量 |
| `/记忆 最近` | 查看关于主人的活跃长期记忆和短编号 |
| `/记忆 候选` / `/记忆 归档` | 查看尚待巩固或已经自然归档的记忆 |
| `/记忆 搜索 关键词` | 搜索关于主人的活跃记忆；被实际取用的结果会轻微强化 |
| `/记忆 添加 身份\|偏好\|事实\|经历\|待续\|约定 内容` | 手工加入一条可信且默认钉住的长期记忆 |
| `/记忆 修改 编号 新内容` | 建立修订版本并把旧内容保留为历史 |
| `/记忆 遗忘 编号` | 从数据库彻底删除一条记忆及其证据 |
| `/记忆 钉住 编号` / `/记忆 取消钉住 编号` | 控制一条记忆是否抵抗自然衰减 |
| `/记忆 巩固` | 立即运行一次候选巩固、强度衰减与过期归档 |
| `/记忆 导出` | 把关于主人的记忆导出为本地 JSON 文件 |
| `/记忆 清空 确认` | 彻底删除关于主人的全部长期记忆及证据 |

英文别名为 `/memory`。`/reset` 只重置当前短期对话，不会删除长期记忆；需要删除时必须明确使用 `/记忆 遗忘` 或 `/记忆 清空 确认`。

### 3.5 行为规划器

行为规划器位于语言生成之前：她会先判断这一轮应该安慰、回答、帮忙、庆祝、观察图片、自然接话还是轻量参与群聊，再把对应的语气、主动程度和长度要求交给模型。因此它会影响日常言行，但不会替代群聊注意力系统——群聊仍先决定“要不要接话”，规划器再决定“接话时应当怎么做”。状态保存在 `qq_sessions/behavior_planner.json`，重启后会保留模式、暂停时间和近期行为记录。

主动私聊、自动空间动态和刚完成的主人私聊会经过同一套防撞仲裁。默认在一次真实互动或主动行为成功后等待 30 分钟，避免刚聊完天又立刻发问候或空间动态；正在生成的主动行为也不会并发。主人手动使用 `/proactive now`、`/动态 草稿` 或 `/动态 发布` 时仍以主人的明确指令为优先，不受自动防撞限制。暂停只影响主动行为，收到消息后的正常回复永远不会被它拦住。

以下命令仅限主人在与她的 QQ 私聊中使用：

| 命令 | 作用 |
|---|---|
| `/行为 状态` | 查看开关、模式、防撞间隔、执行中行为和最近结果 |
| `/行为 开启` / `/行为 关闭` | 开启或关闭行为规划；关闭后保留原有回复与调度流程 |
| `/行为 模式 平衡` | 默认模式；偶尔撒娇、称一次“主人”，有自然承接点时补一句相关内容或必要追问；“喵”、颜文字、耳尾动作不会每条都出现 |
| `/行为 模式 谨慎` | 回复更短克制；很少撒娇和使用颜文字，不重复称呼，不主动追加新话题；吃醋只会含蓄带过 |
| `/行为 模式 活泼` | 主人私聊中更愿意撒娇、称呼和延伸一句相关话题，可自然使用一个“喵”、颜文字或耳尾动作；只有私聊且确实脆弱或深度撒娇时才偶尔称“爸爸” |
| `/行为 暂停 分钟` | 暂停自动主动行为 1～1440 分钟，直接回复不受影响 |
| `/行为 恢复` | 立即恢复自动主动行为 |
| `/行为 最近` | 查看最近 12 条行为计划、意图、状态和理由 |
| `/行为 重置` | 清空行为历史和未完成预约，保留开关与模式 |

英文别名为 `/behavior`。默认防撞间隔可通过 `.env` 中的 `QQ_BEHAVIOR_OUTBOUND_MIN_GAP=1800` 调整，单位为秒。

模式也会影响她吃醋时表现得多明显：谨慎模式几乎只用一句轻微在意带过，平衡模式允许一点酸意或尾巴动作，活泼模式可以更明显但仍然柔软地撒娇。所有模式都禁止责怪主人、贬低别人、要求主人二选一、限制主人的关系或借“害怕被抛弃”施压。模式只改变已经决定执行的内容怎样表达，不改变主动私聊、自动空间动态的候选时间、触发概率、每日上限或勿扰规则。

世界书向量模型默认会在 Bot 启动后立刻后台预热，不再等第一条消息才加载。日志出现“向量检索预热完成”后，首轮对话可以直接检索。`/world status` 可查看预热状态，主人也可以使用 `/world warmup` 手动重新预热。若希望启动流程等待模型完全就绪，可把 `.env` 中的 `WORLD_BOOK_PRELOAD_BACKGROUND` 设为 `false`；默认 `true` 会让 QQ 连接与模型加载并行进行。

## 4. 风险管理命令

风险记录按“群号 + QQ号”保存。主人永远豁免自动风险计数。

| 命令 | 权限 | 作用 |
|---|---|---|
| `/risk` | 主人 | 列出本群风险记录 |
| `/risk QQ号` | 主人 | 查看指定群友在本群的风险次数与状态 |
| `/riskreset QQ号` | 主人 | 删除本群风险计数，同时清除该群友的社交警戒状态 |
| `/riskblock QQ号` | 主人 | 立即把指定群友设为已屏蔽 |
| `/riskunblock QQ号` | 主人 | 解除屏蔽、风险清零并清除社交警戒状态 |

默认阈值为 3：

1. 第一次越界：明确提醒并累计为 1。
2. 第二次越界：冷淡警告并累计为 2。
3. 第三次越界：累计为 3，并开始静默忽略该用户。

风险检测发生在注意力筛选之前，因此即使她本来不准备接话，辱骂、性骚扰、索取隐私和越狱诱导仍会计数。NapCat 重发同一个消息 ID 时不会重复累计；三条不同消息即使文字相同也会分别计数。

## 5. 世界书命令

世界书文件建议放在：

```text
qq_workspace/worldbooks/
```

### 5.1 查询命令

| 命令 | 权限 | 作用 |
|---|---|---|
| `/world` | 所有人 | 显示当前群世界书状态 |
| `/world status` | 所有人 | 查看系统、Qdrant 和本群已绑定世界书状态 |
| `/world list` | 所有人 | 列出已导入世界书、条目数、版本和绑定状态 |

### 5.2 管理命令

| 命令 | 权限 | 作用 |
|---|---|---|
| `/world import 文件或目录 [名称]` | 主人 | 导入世界书文件或目录；名称可以省略 |
| `/world use 名称` | 主人 | 在当前群启用指定世界书 |
| `/world use 名称 trpg` | 主人 | 仅在当前群跑团期间启用指定世界书 |
| `/world reload [名称]` | 主人 | 重载一本世界书；省略名称时重载全部 |
| `/world disable [名称]` | 主人 | 停用当前群的指定绑定；省略名称时停用当前群全部绑定 |

示例：

```text
/world import 蒙德世界书.json 蒙德
/world use 蒙德
/world use 克苏鲁设定 trpg
/world reload 蒙德
/world disable 蒙德
```

补充说明：

- 导入并不等于启用，导入后通常还需要 `/world use 名称`。
- 世界书绑定按群保存；全局常驻世界书由程序配置决定。
- Qdrant 离线时，关键词和永久规则仍可降级使用，但语义向量召回不可用。
- 世界书更新后使用 `/world reload`，系统只会重新索引发生变化的条目。

## 6. 图片识别与发图

### 6.1 查看服务状态

```text
/vision
/vision status
```

返回内容包括：

- 图片识别是否启用；
- Qwen3-VL/Ollama 是否在线；
- PaddleOCR 是否在线；
- 已缓存图片数量。

### 6.2 识别群聊图片

在群聊中，发送图片后配合以下任一方式：

```text
@未名子 看看这张图
未名子，图里写了什么？
/ai 分析这张截图
回复一张图片并说：看看
```

以下关键词通常能触发图片理解：

```text
看看、看一下、识别、分析、读图、这是什么、图里、图片、截图、照片、
写了什么、有什么字、OCR、地图、角色卡、线索、表情包、梗图
```

跑团进行中，发给她的图片会自动尝试识别。每轮只处理第一张有效图片，结果按图片哈希缓存。

### 6.3 让她发送图片或表情包

这是自然语言工具请求，不是固定斜杠命令：

```text
@未名子 发一张开心的猫猫表情包
@未名子 来张生气的反应图
@未名子 发个 hug 表情包
```

她会搜索外部图片源并用 QQ 图片消息发送。外部图片源不可用、链接被拒绝或 NapCat 图片接口失败时，她应明确说明失败，而不是假装发送成功。

## 7. 语音回复

语音是自然语言工具请求，使用前需要先启动 TTS 服务：

```text
start_tts.bat
```

群里可以这样说：

```text
@未名子 用语音说“主人晚上好”
@未名子 把这句话转成 QQ 语音发出来
```

语音生成依赖 `.env` 中的参考音频和提示文本配置。生成成功后，她会通过 QQ `record` 消息发送本地 WAV 文件。

## 8. 跑团命令与自然语言操作

### 8.1 开团

当前没有固定的 `/startgame` 命令。请直接用自然语言并确保直接触发她：

```text
@未名子 开启跑团模式，使用 CoC 7版，我来当 GM
@未名子 开一个 D&D 5e 团，当前场景是一间废弃教堂
```

她会在内部调用团务工具创建当前群的跑团会话。内部工具名如 `trpg_game_session action=start` 是模型调用格式，不是建议群友直接发送的斜杠命令。

### 8.2 固定跑团管理命令

| 命令 | 权限 | 作用 |
|---|---|---|
| `/pausegame`、`/pause`、`/暂停` | 主人/GM | 暂停跑团，保留角色、场景、回合和战斗数据，并重置当前群 Agent 上下文 |
| `/resumegame`、`/resume`、`/继续` | 主人/GM | 恢复已暂停的跑团 |
| `/endgame`、`/endtrpg`、`/结团`、`/关团` | 主人/GM | 结束当前团并重置当前群 Agent 上下文 |

当前代码会保证主人的这些命令直接执行，但跑团管理分支本身没有再次校验 GM 身份；普通群友如果通过 `@她` 等方式直接触发，也可能修改团务。因此应只让可信任的主人/GM使用，后续若开放公共群建议再增加 GM 白名单。

`/endgame` 会把团务状态设为结束，但不会自动删除已有角色卡文件。

### 8.3 角色卡与检定

以下都使用自然语言：

```text
@未名子 创建一张 CoC 7版角色卡，名字叫林默，职业是记者
@未名子 列出本群所有角色
@未名子 查看林默的角色卡
@未名子 让林默进行一次困难侦查检定
@未名子 林默受到 3 点伤害，SAN 减少 1
@未名子 给林默的物品栏加入一把旧钥匙
@未名子 删除角色林默
```

支持的内部能力包括：

- 创建、查看、列出和删除角色卡；
- CoC 7th 与 D&D 5e 属性/技能检定；
- 更新 HP、SAN、状态、物品和属性；
- 为 PC 与 NPC 投先攻并保存顺序；
- 推进到下一行动者和下一回合；
- 设置场景、添加 NPC、更新 NPC、结束战斗；
- 生成随机遭遇、战利品、NPC、名字和疯狂症状。

### 8.4 玩家行动

跑团处于进行状态时，下面这类内容会被识别成玩家行动并要求她主持推进：

```text
我调查桌上的血迹
我对守卫进行心理学检定
我打开地下室的门
我攻击最近的敌人
我躲到柱子后面
```

普通叙事或闲聊如果没有明确行动含义，仍可能按群聊注意力逻辑处理。

## 9. 娱乐与实用自然语言指令

这些不是固定斜杠命令，建议通过 `@她` 或触发前缀发送。

| 能力 | 示例 |
|---|---|
| 掷骰子 | `@未名子 掷 2d6`、`/ai 掷 1d20+3` |
| 今日运势 | `@未名子 给小明抽个今日运势` |
| 神奇八号球 | `@未名子 用八号球回答我今天要不要出门` |
| 随机选择 | `@未名子 从火锅、烧烤、炒饭里随机选一个` |
| 随机话题 | `@未名子 群里冷场了，出个话题` |
| 网页搜索 | `@未名子 搜一下今天的相关资料` |
| 阅读网页 | `@未名子 看看这个链接讲了什么：https://...` |
| 文件列表 | `@未名子 列出工作区里的文件` |
| 读取文件 | `@未名子 读取 worldbooks/README.md` |
| 写入文件 | `@未名子 把下面内容保存到 notes/test.md` |
| 主人身份确认 | 群友问“谁是你的主人”时，她会使用 QQ ID 本地核验 |

QQ 模式下文件操作被限制在 `QQ_WORKSPACE_DIR`，默认是 `qq_workspace/`，不能访问整个电脑，也不能执行 Shell 命令。

## 10. 私聊命令

| 命令 | 权限 | 作用 |
|---|---|---|
| `/reset` | 当前私聊用户 | 重置自己的私聊 Agent 上下文 |
| `/status` | 当前私聊用户 | 查看自己的消息数、估算 token 和空闲时间 |
| `/vision`、`/vision status` | 当前私聊用户 | 查看图片识别服务状态 |

私聊中直接发送图片或回复图片即可触发识别，不要求额外 `@`。私聊会话按 QQ 号隔离。

世界书的永久规则和已配置的全局世界书仍可能参与私聊检索，但 `/world ...` 管理命令只在群聊入口处理。

## 11. 启动脚本

所有脚本都位于项目根目录。

| 脚本 | 作用 |
|---|---|
| `start_qqbot.bat` | 启动 QQBot 主程序；需要 NapCat 的 WebSocket 与 HTTP 服务已运行 |
| `start_vision.bat` | 启动 Docker Desktop、PaddleOCR 和 Ollama，并检查/下载 `qwen3-vl:2b` |
| `start_tts.bat` | 启动 GPT-SoVITS API；首次加载模型可能较慢 |
| `start_pet.bat` | 启动桌面宠物 |
| `start_ui.bat` | 启动项目 UI |
| `start_wechat.bat` | 启动独立的微信机器人入口 |

推荐启动顺序：

```text
1. 启动 NapCatQQ
2. 双击 start_vision.bat（需要图片识别时）
3. 如需世界书向量检索，执行 docker compose -f infra/qdrant/compose.yaml up -d
4. 双击 start_tts.bat（需要语音时）
5. 双击 start_qqbot.bat
```

如果只需要普通文字聊天，可以不启动视觉和 TTS 服务。Qdrant 离线时世界书会降级，Ollama/OCR 离线时图片理解不可用，但 QQBot 主体仍可启动。

## 12. 各类“重置”的区别

| 操作 | 对话历史 | 全局关系 | 当前情绪 | 风险计数 | 世界书绑定 | 跑团/角色卡 |
|---|---:|---:|---:|---:|---:|---:|
| 群聊 `/reset` | 清当前群 | 保留 | 保留 | 保留 | 保留 | 保留 |
| 私聊 `/reset` | 清当前私聊 | 保留 | 保留 | 不适用 | 保留 | 不适用 |
| `/clearall` | 清全部 Agent 会话 | 保留 | 保留 | 保留 | 保留 | 保留 |
| `/socialreset QQ号` | 保留 | 清指定用户 | 保留 | 保留 | 保留 | 保留 |
| `/mood reset` | 保留 | 保留 | 重置 | 保留 | 保留 | 保留 |
| `/riskreset QQ号` | 保留 | 保留熟悉度，清警戒 | 保留 | 清当前群指定用户 | 保留 | 保留 |
| `/world disable` | 保留 | 保留 | 保留 | 保留 | 停用绑定 | 保留 |
| `/endgame` | 重置当前群 Agent 上下文 | 保留 | 保留 | 保留 | 保留 | 结束团务，角色卡保留 |

## 13. 常用流程速查

### 导入并启用日常世界书

```text
把文件放入 qq_workspace/worldbooks/
/world import 文件名.json 世界书名称
/world use 世界书名称
/world status
```

### 开始带世界书的跑团

```text
/world use 世界书名称 trpg
@未名子 开启跑团，使用 CoC 7版，我是 GM
@未名子 当前场景是一座废弃医院
```

### 暂停与恢复跑团

```text
/pause
/resume
```

### 检查某个群友为什么不再收到回复

```text
/risk QQ号
/social QQ号
```

如需完全恢复：

```text
/riskunblock QQ号
```
