# 未名子桌面 Agent 新架构设计

## 1. 设计结论

新版不继承原有 `pywebview + 本地 HTTP + JavaScript 桥接 + Live2D + Node` 桌宠实现。

未名子的本体是一个独立、长期运行的 Agent 核心；桌面形象只是可以关闭和重启的显示终端。QQ、未来的其他聊天渠道也只是终端，不拥有她的记忆和生命周期。

## 2. 进程边界

### 未名子核心进程

核心进程负责：

- 唯一身份和运行实例；
- 长期记忆、短期会话和世界书；
- 生活状态、活动账本和行为规划；
- 目标、任务、步骤、失败重试和恢复；
- 工具权限、主人确认和审计记录；
- 定时事件、电脑休眠与唤醒后的时间结算；
- 向所有显示或通信终端发布统一状态。

核心不创建桌面窗口，不依赖 QQ，不依赖浏览器运行时。桌面前端退出后核心仍继续运行。

### 原生 Windows 显示进程

显示进程只负责：

- 显示角色头像、轻量帧动画和状态变化；
- 显示对话气泡、输入框、任务与确认请求；
- 窗口拖动、置顶、隐藏和多显示器位置记忆；
- 把主人输入发送给核心，并显示核心返回的事件流。

第一版使用原生 Windows 窗口和普通 PNG/序列帧，不使用 WebView、HTML、JavaScript、Live2D 或内嵌 HTTP 服务。动画资源以后通过独立渲染适配层扩展，不能反向侵入核心。

### 通信方式

核心与显示层使用仅限当前 Windows 用户访问的命名管道：

- 每个消息包含版本、事件编号、类型、时间和负载；
- 支持请求、流式回复、状态订阅、确认和取消；
- 前端断线可重新订阅，不会中断核心任务；
- 事件编号用于去重，防止重连后重复执行操作；
- 外部渠道不能直接访问工具，只能向核心提交事件。

## 3. Agent 生命周期

核心采用事件驱动，不让模型持续空转：

1. 本地调度器或终端提交事件；
2. 注意力层判断是否需要处理；
3. 行为规划器确定意图、语气和是否需要建立任务；
4. 任务规划器把复杂目标拆成可恢复步骤；
5. 权限层判断自动执行、请求确认或拒绝；
6. 执行器运行一个步骤并保存真实结果；
7. 活动账本记录事实，记忆系统只从已验证结果中学习；
8. 核心发布状态，随后回到低资源等待。

普通时间推进、精力变化、定时检查和任务状态维护不调用大模型。只有需要理解、规划、生成或复杂判断时才调用模型。

## 4. 持久化与恢复

新增统一运行数据库 `agent_runtime.db`，使用 SQLite WAL 模式，保存：

- core_instance：启动、心跳、休眠和恢复信息；
- goals：主人明确建立的目标；
- tasks：可暂停、继续和取消的任务；
- task_steps：步骤、输入、输出、重试和幂等键；
- events：来自桌面、计时器和渠道的事件；
- approvals：待确认操作及有效期；
- subscriptions：终端订阅和最后确认事件编号；
- runtime_settings：安静模式、权限级别和资源策略。

现有 `long_term_memory.db`、`activity_ledger.db`、生活状态和行为规划数据继续保留，不迁入显示层。核心启动时清理过期执行租约，把中断步骤恢复为“等待重试”，不会把未知结果直接当作成功。

## 5. 权限模型

| 等级 | 能力 |
|---|---|
| 0 | 读取自身状态、记忆和活动账本 |
| 1 | 读取主人明确授权的目录 |
| 2 | 在未名子专属工作区创建和修改文件 |
| 3 | 修改主人文件、启动程序或改变电脑状态，执行前确认 |
| 4 | 发送外部消息、发布内容、删除数据等操作，每次确认 |

权限按照“工具 + 路径/对象 + 有效期”授予，不提供无限制的总开关。所有副作用必须具有幂等键、结果记录和可恢复策略；能撤销的操作应保存撤销信息。

## 6. 长时间在线策略

- Windows 用户登录时启动核心，而不是作为无法访问用户桌面的系统服务；
- 使用命名互斥量保证只有一个核心实例；
- 心跳只写本地状态，不调用模型；
- 电脑休眠时停止计时任务，唤醒后按真实离线时间结算；
- 网络或模型不可用时进入降级状态，本地记忆、日程和任务队列仍可使用；
- 重型视觉、语音和向量模型按需启动并由资源协调器回收；
- 显示层可以任意关闭、重开或升级，不影响核心在线时间。

## 7. 实施阶段

### 第一阶段：常驻核心

- 建立单实例核心、运行数据库、事件总线和安全退出；
- 接入现有记忆、生活状态、活动账本和行为规划；
- 提供命令行诊断终端；
- 连续运行 24 小时并验证休眠、唤醒和崩溃恢复。

### 第二阶段：任务型 Agent

- 实现目标、任务、步骤、审批和失败恢复；
- 首批只开放查资料、生成文档和管理专属工作区；
- 所有实际动作进入活动账本，计划不能冒充经历。

### 第三阶段：原生显示层

- 创建独立原生 Windows 透明窗口；
- 使用未名子自己的 PNG/序列帧，而不是旧 Live2D 模型；
- 接入聊天、状态、任务和确认界面；
- 进行多显示器、缩放、窗口隐藏和前端崩溃测试。

### 第四阶段：可选感知和渠道

- 每一种屏幕、剪贴板、活动窗口或目录观察能力单独授权；
- QQ 等渠道作为可禁用适配器接入；
- 渠道离线不得影响核心和桌面交互。

## 8. 第一版验收标准

- QQ、桌面前端全部关闭后，核心仍保持同一身份和任务状态；
- 前端重启后恢复同一段会话、生活状态和未完成任务；
- 核心连续运行 24 小时无重复实例、无模型空转、无任务重复执行；
- 网络断开和电脑休眠不会损坏数据库；
- 未经授权不能读取指定范围外文件或执行外部副作用；
- 任一实际行为都能在活动账本中追溯到事件、计划、权限和结果。

## 9. 当前已实现的首版边界

首版实现于 `desktop_agent_core.py` 与 `desktop_agent/Unnameko.Desktop`：

- Windows 命名互斥量保证核心单实例；命名管道 `unnameko-agent-v1` 提供状态、聊天、重置和安全停止；
- `agent_runtime.db` 记录每次核心启动、30 秒心跳、安全停止原因和运行错误；
- 桌面会话、生活状态和行为规划使用 `agent_sessions` 下的独立文件，避免与现有 QQ 进程争用 JSON；
- 长期记忆和活动账本复用现有 SQLite WAL 数据库，让同一个未名子跨入口保持连续；
- WPF 窗口可隐藏到托盘、自动重连并在核心未运行时自动启动核心；
- 首版工具白名单只有目录/文件读取、联网查资料、语音和无副作用随机工具，写文件、运行命令、TRPG 状态修改和电脑控制均未开放。

覆盖/删除类操作、多显示器动画与感知能力仍属于后续阶段，不能把本首版描述为已经具备这些能力。

## 10. 任务与审批首版（已实现）

`agent_runtime.db` 现已加入 `agent_goals`、`agent_tasks`、`agent_task_steps` 和
`agent_approvals`。WPF 的“任务”页可以建立一个写入专属工作区的任务，并显示一次性权限卡片。

任务页现已支持自然语言目标规划。规划器输出可持久化的 `draft` 计划，WPF 完整展示步骤后由
主人确认；确认前不创建审批、不执行步骤。模型不可用或返回无效 JSON 时，核心会退回本地
保守计划。安全执行器只允许 `content.prepare` 与 `workspace.write_text`：

- 文件必须位于 `agent_workspace`，且必须是尚不存在的新文本文件；
- 绝对路径、父目录跳转、隐藏目录和脚本/可执行后缀会在创建任务前被拒绝；
- 步骤必须先处于 `waiting_approval`，主人批准后才能从 `ready` 进入 `running`；
- 批准只对一个 `approval_id` 和一个 `step_id` 有效，不能重复使用；
- 多文件计划按顺序推进，每一个 `workspace.write_text` 都会生成新的独立审批；
- 拒绝或过期会同步取消目标、任务和步骤；
- 核心重启时会检查中断步骤；若批准后的文件已经以相同内容完整落盘，则按幂等恢复为完成，否则安全重试或失败，不覆盖未知文件。

尚未开放任意命令、启动程序、覆盖文件、删除、修改主人目录或外部发送。这些动作必须在后续版本中逐种加入独立执行器、风险说明和审批卡片。

## 11. 任务系统 2.0 与桌面控制面（已实现）

- 规划器会记录来源、耗时和降级原因，支持草案重生成与主人修订；模型规划超时与普通聊天超时分离；
- `web.research` 与 `document.compose` 是只读自动步骤，最终文档物化后才建立写入审批；
- `workspace.update_text` 与 `workspace.append_text` 仅作用于专属工作区 UTF-8 小文本，审批卡展示差异，执行前核对原哈希并写入 `.versions` 备份；
- `workspace.create_directory` 与 `workspace.rename` 逐步骤审批，删除、脚本和专属工作区外写入继续禁止；
- 任务可暂停、继续、取消和有限重试，耗时任务由本地互斥队列串行推进；
- `agent_reminders` 保存主人主动建立的提醒，提醒不属于自主联系倾向；
- 记忆页直接使用长期记忆生命周期的列出、修订、置顶和硬删除接口；
- 剪贴板、窗口标题、截图均为前端逐次授权，预览后随一轮消息使用；临时截图识别后删除。
